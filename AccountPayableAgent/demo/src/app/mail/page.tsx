import { VendorMail } from "@/components/vendor-mail";

export const metadata = {
  title: "Mail",
  description: "Vendor mailbox",
};

export default function MailPage() {
  return <VendorMail />;
}
