import { DiagnosticsClient } from "./diagnostics-client";

export const metadata = { title: "PayablePilot · Diagnostics" };

export default function DiagnosticsPage() {
  return <DiagnosticsClient />;
}
