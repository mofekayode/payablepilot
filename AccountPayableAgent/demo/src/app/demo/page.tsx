import { AppShell } from "@/components/app-shell";

export const metadata = {
  title: "PayablePilot · Demo",
  description: "Narrative walkthrough of the PayablePilot AP agent on a sample property-management portfolio.",
};

export default function DemoPage() {
  return <AppShell />;
}
